/**
 * Placeholder types. In a real Supabase setup with the CLI, run:
 *   npx supabase gen types typescript --project-id <ref> > lib/supabase/types.ts
 * to generate the full schema from your live project.
 */
export type Database = any;
